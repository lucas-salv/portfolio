type projectType = {
    name: string,
    description: string,
    tools: string[],
    links: { website_url: string, repo_url: string },
    background_image_url: string,
    photos: string[]
}

export default projectType

